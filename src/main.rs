#[derive(Debug)]
use std::collections::HashMap;

struct Board {
    rank: HashMap<String, >
    // seeds: i32,
    // hole: i32,
    // player: i32,
    // mancala: bool
    //next: Option<Box<Board>>
}

impl Board {
    fn new() -> Self {
        let players = [0, 1];
        let pits = [0, 1, 2, 3, 4, 5, 6];

        for player in players {
            for pit in pits {
                let 
            }
        }
    }
}

fn main() {
    let _board = Board::new();
}
